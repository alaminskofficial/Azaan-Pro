import { View, Text } from "react-native";

export default function Settings() {
  return (
    <View style={{ padding: 20 }}>
      <Text style={{ color : 'white'}}>Settings (Calculation Method, Madhab, etc.)</Text>
    </View>
  );
}